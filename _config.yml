defaults:
  - scope:
      path: ""
    values:
      layout: "default"
  - scope:
      path: ""
      type: "blog"
    values:
      layout: "blog"

collections_dir: _collections
collections:
  blog:
    output: true

sass:
  sass_dir: css/_includes

future: true
theme: null
exclude:
  - scss

# detach: false
